{{>layout/header}}

<main class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="flex justify-between items-center mb-8">
        <h1 class="text-3xl font-bold text-gray-800">📋 문의사항</h1>
        <a href="/articles/new" class="px-6 py-2 text-white bg-primary-blue rounded-lg hover:bg-blue-600 font-semibold transition duration-150">✏️ 새 문의</a>
    </div>

    <div class="bg-white rounded-xl shadow-lg overflow-hidden">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
            <tr>
                <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider w-1/6">작성자</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-3/6">제목</th>
                <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider w-2/6">작성 일자</th>
            </tr>
            </thead>
            <tbody class="divide-y divide-gray-200">
            {{#articleList}}
                <tr onclick="location.href='/articles/view/{{id}}'" class="hover:bg-gray-50 transition duration-150 cursor-pointer">
                    <td class="px-6 py-4 whitespace-nowrap text-center text-sm font-medium text-gray-900">{{memName}}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700">🔒 {{title}}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-center text-sm text-gray-500">{{createdAt}}</td>
                </tr>
            {{/articleList}}

            {{^articleList}}
                <tr>
                    <td colspan="3" class="px-6 py-10 text-center text-base text-gray-500">
                        등록된 게시글이 없습니다 😢
                    </td>
                </tr>
            {{/articleList}}
            </tbody>
        </table>
    </div>
    {{#totalPages}}
        <div class="flex justify-center mt-8 space-x-2">
            {{#pages}}
                {{#isCurrent}}
                    <span class="px-4 py-2 bg-primary-blue text-white rounded-md text-sm font-semibold">
                        {{number}}
                    </span>
                {{/isCurrent}}
                {{^isCurrent}}
                    <a href="/articles/list?page={{index}}"
                    class="px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-md text-sm text-gray-700">
                    {{number}}
                    </a>
                {{/isCurrent}}
            {{/pages}}
        </div>
    {{/totalPages}}
</main>

{{#msg}}
    <script>
        alert("{{msg}}");
    </script>
{{/msg}}

{{>layout/footer}}