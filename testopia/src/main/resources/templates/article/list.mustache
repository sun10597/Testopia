{{>layout/header}}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold text-primary">📋 게시판</h1>
        <a href="/articles/new" class="btn btn-primary">✏️ 새 글 작성</a>
    </div>

    <table class="table table-hover align-middle shadow-sm">
        <thead class="table-light">
        <tr class="text-center">
            <th scope="col" style="width:10%;">작성자</th>
            <th scope="col">제목</th>
        </tr>
        </thead>
        <tbody>
        {{#articleList}}
            <tr onclick="location.href='/articles/view/{{id}}'" style="cursor:pointer;">
                <td class="text-center fw-semibold">{{memName}}</td>
                <td class="ps-3">{{title}}</td>
            </tr>
        {{/articleList}}

        {{^articleList}}
            <tr>
                <td colspan="2" class="text-center text-muted py-4">
                    등록된 게시글이 없습니다 😢
                </td>
            </tr>
        {{/articleList}}
        </tbody>
    </table>
</div>

{{#msg}}
    <script>
        window.addEventListener("DOMContentLoaded", () => {
            alert("{{msg}}");
        });
    </script>
{{/msg}}

{{>layout/footer}}
