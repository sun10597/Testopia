{{>layout/header}}

<div class="container mt-5">
    <h1 class="fw-bold text-primary mb-4">✏️ 새 글 작성</h1>

    <form method="POST" action="/articles/newProc" class="shadow-sm p-4 rounded bg-light">
        <div class="mb-3">
            <label for="title" class="form-label fw-semibold">글 제목</label>
            <input type="text" class="form-control" id="title" name="title" placeholder="제목을 입력해주세요." required>
        </div>

        <div class="mb-3">
            <label for="content" class="form-label fw-semibold">내용</label>
            <textarea class="form-control" id="content" name="content" rows="8" placeholder="내용을 입력해주세요." required></textarea>
        </div>

        <div class="d-flex justify-content-end gap-2">
            <a href="/articles/list" class="btn btn-secondary">📋 목록</a>
            <button type="reset" class="btn btn-light">🧹 초기화</button>
            <button type="submit" class="btn btn-primary">💾 등록</button>
        </div>
    </form>
</div>

{{#msg}}
    <script>
        alert("{{msg}}");
    </script>
{{/msg}}

{{>layout/footer}}
