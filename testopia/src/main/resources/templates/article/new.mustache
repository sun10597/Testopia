{{>layout/header}}

<main class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <h1 class="text-3xl font-bold text-gray-800 mb-8">✏️ 새 글 작성</h1>

    <form method="POST" action="/articles/newProc" class="bg-white rounded-xl shadow-lg p-6 max-w-2xl mx-auto">
        <div class="mb-6">
            <label for="title" class="block text-lg font-semibold text-gray-700 mb-2">글 제목</label>
            <input type="text" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue"
                   id="title" name="title" placeholder="제목을 입력해주세요." required>
        </div>

        <div class="mb-8">
            <label for="content" class="block text-lg font-semibold text-gray-700 mb-2">내용</label>
            <textarea class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue"
                      id="content" name="content" rows="8" placeholder="내용을 입력해주세요." required></textarea>
        </div>

        <div class="flex justify-end space-x-3">
            <a href="/articles/list" class="px-4 py-2 text-gray-700 bg-gray-200 rounded-lg hover:bg-gray-300 transition duration-150">📋 목록</a>
            <button type="reset" class="px-4 py-2 text-gray-700 bg-gray-100 border border-gray-300 rounded-lg hover:bg-gray-200 transition duration-150">🧹 초기화</button>
            <button type="submit" class="px-6 py-2 text-white bg-primary-blue rounded-lg hover:bg-blue-600 font-semibold transition duration-150">💾 등록</button>
        </div>
    </form>
</main>

{{#msg}}
    <script>
        alert("{{msg}}");
    </script>
{{/msg}}

{{>layout/footer}}