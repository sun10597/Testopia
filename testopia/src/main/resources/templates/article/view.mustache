{{>layout/header}}

<main class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
    {{#articleVO}}
        <h1 class="text-3xl font-bold text-gray-800 mb-8">📖 {{memName}}님 문의사항</h1>

        <div class="bg-white rounded-xl shadow-lg p-6 mb-8 max-w-3xl mx-auto">
            <div class="mb-6">
                <label class="block text-lg font-semibold text-gray-700 mb-2">제목</label>
                <input readonly class="w-full px-4 py-2 border border-gray-200 bg-gray-50 rounded-lg text-gray-800" value="{{title}}">
            </div>

            <div class="mb-6">
                <label class="block text-lg font-semibold text-gray-700 mb-2">내용</label>
                <textarea readonly class="w-full px-4 py-3 border border-gray-200 bg-gray-50 rounded-lg text-gray-800 resize-none" rows="8">{{content}}</textarea>
            </div>
        </div>

        <div class="flex justify-end space-x-3 max-w-3xl mx-auto">
            <a href="/articles/list" class="px-4 py-2 text-gray-700 bg-gray-200 rounded-lg hover:bg-gray-300 transition duration-150">📋 목록</a>

            {{#canModify}}
                <a href="/articles/edit/{{id}}" class="px-4 py-2 text-white bg-primary-blue rounded-lg hover:bg-blue-600 transition duration-150">✏️ 수정</a>
                <a href="/articles/delete/{{id}}" class="px-4 py-2 text-white bg-red-500 rounded-lg hover:bg-red-600 transition duration-150"
                   onclick="return confirm('정말 삭제하시겠습니까?');">🗑 삭제</a>
            {{/canModify}}
        </div>
    {{/articleVO}}
</main>

{{#msg}}
    <script>
        alert("{{msg}}");
    </script>
{{/msg}}

{{>layout/footer}}