{{>layout/header}}

{{#articleEntity}}
    <div class="container mt-5">
        <h1 class="fw-bold text-primary mb-4">📖 {{member.memName}}님 문의사항</h1>

        <div class="shadow-sm p-4 rounded bg-light mb-4">
            <div class="mb-3">
                <label class="form-label fw-semibold">제목</label>
                <input readonly class="form-control" value="{{title}}">
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold">내용</label>
                <textarea readonly class="form-control" rows="8">{{content}}</textarea>
            </div>
        </div>

        <div class="d-flex justify-content-end gap-2">
            <a href="/articles/list" class="btn btn-secondary">📋 목록</a>
            <a href="/articles/edit/{{id}}" class="btn btn-primary">✏️ 수정</a>
            <a href="/articles/delete/{{id}}" class="btn btn-danger"
               onclick="return confirm('정말 삭제하시겠습니까?');">🗑 삭제</a>
        </div>
    </div>
{{/articleEntity}}

{{#msg}}
    <script>
        alert("{{msg}}");
    </script>
{{/msg}}

{{>layout/footer}}
