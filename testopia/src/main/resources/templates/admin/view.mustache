{{>layout/header}}

<main class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
    {{#memberVO}}
        <h1 class="text-3xl font-bold text-gray-800 mb-8">📖 {{memName}}님 회원정보</h1>

        <div class="bg-white rounded-xl shadow-lg p-6 mb-8 max-w-3xl mx-auto">
            <div class="mb-6 border-b pb-4">
                <label class="block text-lg font-semibold text-gray-700 mb-1">회원 ID</label>
                <p class="text-gray-900 text-base">{{memId}}</p>
            </div>
            <div class="mb-6 border-b pb-4">
                <label class="block text-lg font-semibold text-gray-700 mb-1">이메일</label>
                <p class="text-gray-900 text-base">{{memEmail}}</p>
            </div>
            <div class="mb-6 border-b pb-4">
                <label class="block text-lg font-semibold text-gray-700 mb-1">역할 (Role)</label>
                <p class="text-gray-900 text-base font-bold">{{role}}</p>
            </div>
            <div class="mb-6 border-b pb-4">
                <label class="block text-lg font-semibold text-gray-700 mb-1">가입 경로</label>
                <p class="text-gray-600 text-sm">{{provider}}</p>
            </div>
            <div class="mb-0">
                <label class="block text-lg font-semibold text-gray-700 mb-1">가입/최종 수정 일자</label>
                <p class="text-gray-600 text-sm">가입: {{CreatedAt}} / 수정: {{UpdatedAt}}</p>
            </div>
        </div>

        <div class="flex justify-end space-x-3 max-w-3xl mx-auto">
            <a href="/admin/members/list" class="px-4 py-2 text-gray-700 bg-gray-200 rounded-lg hover:bg-gray-300 transition duration-150">📋 목록</a>

            <a href="/admin/members/edit/{{memId}}" class="px-4 py-2 text-white bg-primary-blue rounded-lg hover:bg-blue-600 transition duration-150">✏️ 수정</a>

            <a href="/admin/members/delete/{{memId}}" class="px-4 py-2 text-white bg-red-500 rounded-lg hover:bg-red-600 transition duration-150"
               onclick="return confirm('{{memName}} 회원을 정말 삭제하시겠습니까?');">🗑 삭제</a>
        </div>
    {{/memberVO}}
</main>

{{#msg}}
    <script>
        alert("{{msg}}");
    </script>
{{/msg}}

{{>layout/footer}}