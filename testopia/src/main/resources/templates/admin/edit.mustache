{{>layout/header}}

<main class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
    {{#memberVO}}
        <h1 class="text-3xl font-bold text-gray-800 mb-8">📝 회원 수정</h1>

        <form method="POST" action="/admin/members/editProc" class="bg-white rounded-xl shadow-lg p-6 max-w-2xl mx-auto">
            <input type="hidden" name="memId" value="{{memId}}">

            <div class="mb-6">
                <label for="memName" class="block text-lg font-semibold text-gray-700 mb-2">회원명</label>
                <input type="text" class="w-full px-4 py-2 border border-gray-300 bg-gray-200 rounded-lg"
                       id="memName" name="memName" value="{{memName}}" readonly>
            </div>

            <div class="mb-6">
                <label for="memEmail" class="block text-lg font-semibold text-gray-700 mb-2">이메일</label>
                <input type="text" class="w-full px-4 py-2 border border-gray-300 bg-gray-200 rounded-lg"
                       id="memEmail" name="memEmail" value="{{memEmail}}" readonly>
            </div>

            <div class="mb-6">
                <label for="role" class="block text-lg font-semibold text-gray-700 mb-2">역할 (Role)</label>
                <input type="text" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-primary-blue focus:border-primary-blue"
                       id="role" name="role" value="{{role}}" required>
                <p class="text-sm text-gray-500 mt-1">관리자: 1, 일반 사용자: 0</p>
            </div>

            <div class="flex justify-end space-x-3">
                <a href="/admin/members/list" class="px-4 py-2 text-gray-700 bg-gray-200 rounded-lg hover:bg-gray-300 transition duration-150">⬅️ 뒤로</a>
                <button type="reset" class="px-4 py-2 text-gray-700 bg-gray-100 border border-gray-300 rounded-lg hover:bg-gray-200 transition duration-150">🧹 초기화</button>
                <button type="submit" class="px-6 py-2 text-white bg-primary-blue rounded-lg hover:bg-blue-600 font-semibold transition duration-150">💾 수정 완료</button>
            </div>
        </form>
    {{/memberVO}}
</main>

{{#msg}}
    <script>
        alert("{{msg}}");
    </script>
{{/msg}}

{{>layout/footer}}